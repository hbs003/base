<%= form_for[@carteira, @seguro] do |f| %>
  <div class="form-group">
    <%= f.label :tipo %>
    <%= f.select :tipo, options_for_type, {}, prompt: 'Select One' %>
  </div>

  <div class="form-group">
    <%= f.label :valor %>
    <%= f.text_field :valor, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :data_final %>
    <%= f.text_field :data_final, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :data_final %>
    <%= f.text_field :data_final, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :periodicidade %>
    <%= f.text_field :periodicidade, placeholder: "Puts the URL of one photo here", class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.input :photo %>
    <%= f.input :photo_cache, as: :hidden %>
  </div>

  <%= f.submit class: 'btn btn-primary' %>
<% end %>

